# ── Runtime state (machine-local, never synced) ──────────────
history.jsonl
stats-cache.json
*.cache.json
.last-cleanup
.last-update-result.json
mcp-needs-auth-cache.json

backups/
cache/
chrome/
debug/
downloads/
file-history/
paste-cache/
plans/
projects/
scheduled-tasks/
session-env/
sessions/
shell-snapshots/
statsig/
todos/
uploads/
.claude/
.worktrees/

# ── Plugins (managed by /plugin, not by this repo) ───────────
plugins/

# ── Skills installed via tooling (re-installable) ────────────
skills/

# ── OS ───────────────────────────────────────────────────────
.DS_Store
