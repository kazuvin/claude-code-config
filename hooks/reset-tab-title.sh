#!/bin/bash
# Claude Code UserPromptSubmit hook - タブタイトルリセット
# ユーザーが入力を送信したらタブタイトルから 🔔 を外す

# タブタイトルをリセット（空にするとデフォルトに戻る）
{
  printf '\033]0;\007' > /dev/tty
  printf '\033]0;\007'
} 2>/dev/null || true
